name: Dependabot
on: [push]
jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/cache@v1 # comment
  ci:
    name: Ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/cache@v1.1.2

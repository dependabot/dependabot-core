[package]
name = "dependabot_sub_crate2" # the name of the package
version = "0.2.0"    # the current version, obeying semver
authors = ["support@dependabot.com"]
workspace = "../../"

[dependencies]
dependabot_sub_crate = { path = "../sub_crate" }

terraform {
  required_providers {
    hashicorp-http = {
      source  = "hashicorp/http"
      version = "2.0"
    }
    mycorp-http = {
      source  = "mycorp/http"
      version = "1.0"
    }
  }
}

# frozen_string_literal: true

source "https://rubygems.org"

# NOTE: Used to run native helper specs
group :test do
  gem "debug", ">= 1.0.0"
  gem "rspec", "~> 3.8"
  gem "rspec-its", "~> 1.2"
  gem "vcr", "6.1.0"
  gem "webmock", "~> 3.4"
end

[tool.poetry]
name = "my-project"
version = "0.1.0"
description = ""
authors = []
license = ""
packages = [
    {include = "src"},
]

[tool.poetry.dependencies]
python = "^3.9"

[build-system]
requires = ["poetry>=1.0"]
build-backend = "poetry.masonry.api"

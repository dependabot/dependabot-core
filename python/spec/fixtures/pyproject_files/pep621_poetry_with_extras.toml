[project]
name = "test"
version = "0.1.0"
description = "Test project with PEP 621 + Poetry and extras"
requires-python = ">=3.10"

dependencies = [
    "cachecontrol[filecache]>=0.14.0",
    "requests>=2.32.0",
]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"


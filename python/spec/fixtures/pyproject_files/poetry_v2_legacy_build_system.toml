[project]
name = "legacy-build-project"
version = "0.1.0"
description = "A project using legacy poetry-core>=1.0 build backend with PEP 621"
authors = [
    {name = "Dependabot Support", email = "support@dependabot.com"}
]
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

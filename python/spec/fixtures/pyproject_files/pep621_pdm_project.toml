[project]
name = "my-project"
version = "0.1.0"
description = "A sample PEP 621 project"
authors = [
    {name = "John Doe", email = "john@example.com"},
]
dependencies = [
    "requests==2.25.0",
    "click>=7.0",
]
requires-python = ">=3.8"

[project.optional-dependencies]
dev = [
    "pytest>=6.0",
    "black>=21.0",
]

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"

[tool.pdm]
distribution = true

[project]
name = "pep621-only-project"
version = "0.1.0"
description = "A project using only PEP 621 project.dependencies with Poetry v2"
authors = [
    {name = "Dependabot Support", email = "support@dependabot.com"}
]
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0,<3.0",
    "urllib3>=1.26.0",
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

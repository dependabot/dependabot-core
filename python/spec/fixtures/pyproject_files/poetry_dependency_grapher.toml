[tool.poetry]
name = "test"
version = "0.1.0"
description = "Test project for dependency graphing"
authors = ["Test <test@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"
flask = "^3.1.3"
requests = "^2.32.5"

[tool.poetry.group.dev.dependencies]
ruff = "^0.15.4"

[tool.poetry]
name = "test-project"
version = "0.1.0"
description = "Test project with git dependency that has extras"
authors = ["Test Author <test@example.com>"]

[tool.poetry.dependencies]
python = "^3.9"
pytest = "^3.7"
onetl = {git = "https://github.com/example/onetl.git", rev = "v1.0.0", extras = ["ftp", "s3"]}

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

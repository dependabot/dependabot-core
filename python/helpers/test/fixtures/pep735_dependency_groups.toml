[project]
name = "myapp"
version = "1.0.0"
requires-python = ">=3.10"

dependencies = [
    "requests>=2.13.0",
]

[dependency-groups]
dev = [
    "pytest==7.1.3",
    "black==22.10.0",
]
lint = [
    {include-group = "dev"},
    "flake8>=5.0",
]

<?php

class Updater
{
  public function update()
  {
    // TODO: Write real codes
    //
    // Note: if any pre-processing of the dependency files is necessary, it
    // can be done in Ruby pretty easily (e.g., DpendencyFileUpdater::Ruby
    // changes the version in the Gemfile before opening up Bundler).
    fwrite(STDOUT, "{\"error\": \"Function not implemented!\" }");
    exit(1);
  }
}

?>

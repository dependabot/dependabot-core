#!/usr/bin/env bash

set -e

bundle install
bundle exec turbo_tests --verbose

# TODO: If you have native helpers or additional tests, add them here
# Example:
# cd /opt/pre_commit && npm run lint && cd -
# cd /opt/pre_commit && npm test && cd -

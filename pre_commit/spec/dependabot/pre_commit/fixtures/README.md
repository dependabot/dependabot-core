# Test Fixtures

This directory contains test fixtures for the pre_commit ecosystem.

Add sample manifest files, lockfiles, and other test data here.

Example structure:
```
fixtures/
├── manifest.json
├── lockfile.lock
└── projects/
    ├── simple/
    └── complex/
```

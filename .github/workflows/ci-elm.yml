name: CI Elm
on:
  push:
    branches:
      - "main"
    paths:
      - Dockerfile
      - 'common/**'
      - 'elm/**'
  pull_request:
    branches:
      - "main"
    paths:
      - Dockerfile
      - 'common/**'
      - 'elm/**'
  schedule:
    - cron: "0 0 * * *"

jobs:
  ci:
    name: CI
    uses: ./.github/workflows/ci.yml
    with:
      name: ${{ matrix.suite.name }}
      path: ${{ matrix.suite.path }}
    strategy:
      fail-fast: false
      matrix:
        suite:
          - { path: elm, name: elm }

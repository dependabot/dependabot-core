name: CI Gradle
on:
  push:
    branches:
      - "main"
    paths:
      - Dockerfile
      - 'common/**'
      - 'gradle/**'
  pull_request:
    branches:
      - "main"
    paths:
      - Dockerfile
      - 'common/**'
      - 'gradle/**'
  schedule:
    - cron: "0 0 * * *"

jobs:
  ci:
    name: CI
    uses: ./.github/workflows/ci.yml
    with:
      name: ${{ matrix.suite.name }}
      path: ${{ matrix.suite.path }}
    strategy:
      fail-fast: false
      matrix:
        suite:
          - { path: gradle, name: gradle }

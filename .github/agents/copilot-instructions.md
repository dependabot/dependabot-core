# dependabot-core Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-18

## Active Technologies

- Nix (flakes, nix2container library); Ruby 3.4.x (existing runtime); Bash (build scripts) + nix2container (OCI image builder), nixpkgs (package set, pinned revision), Nix flakes (001-nix-container-migration)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Nix (flakes, nix2container library); Ruby 3.4.x (existing runtime); Bash (build scripts)

## Code Style

Nix (flakes, nix2container library); Ruby 3.4.x (existing runtime); Bash (build scripts): Follow standard conventions

## Recent Changes

- 001-nix-container-migration: Added Nix (flakes, nix2container library); Ruby 3.4.x (existing runtime); Bash (build scripts) + nix2container (OCI image builder), nixpkgs (package set, pinned revision), Nix flakes

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->

[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"

[project]
name = "dummy-project"
version = "0.1.0"
description = ""
requires-python = ">=3.9"
dependencies = [
    "requests>=2.31.0",
]

[project]
name = "non-explicit-index-project"
version = "0.1.0"
description = "Test UV non-explicit index configuration"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.31.0",
    "another-package>=1.0.0",
]

[[tool.uv.index]]
name = "company_pypi"
url = "https://private-pypi.example.com/simple"

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

#!/usr/bin/env bash

set -e

bundle install
bundle exec turbo_tests --verbose

# TODO: If you have native helpers or additional tests, add them here
# Example:
# cd /opt/luarocks && npm run lint && cd -
# cd /opt/luarocks && npm test && cd -
